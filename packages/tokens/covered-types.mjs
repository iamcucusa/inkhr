// The token types the package covers: the build outputs them and the description rule checks them.
// A slice adds its type here in the same pull request as the type's descriptions and transform.
export const COVERED_TYPES = ['color'];
